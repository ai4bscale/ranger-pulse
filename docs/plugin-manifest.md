# Plugin manifest (draft)

Placeholder — the manifest schema is defined properly as part of
[`specs/001-mvp-vertical-slice/spec.md`](../specs/001-mvp-vertical-slice/spec.md)
(FR3, FR11, FR12) rather than here, so the spec stays the single source
of truth while it's still being clarified.

Once `/speckit.plan` turns that spec into a concrete technical plan, this
file becomes the human-readable reference for anyone writing a plugin —
collector output schema, the declarative UI-rendering fields (widget
type, thresholds, labels), and why plugins never ship executable UI code
(constitution, Article III).
