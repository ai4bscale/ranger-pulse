# Go build artifacts
/bin/
/dist/
*.exe
*.test
*.out

# Go module download cache (never commit)
/vendor/

# Local env / secrets — Ranger-Pulse should never need a .env committed
.env
*.local.yaml

# Editor / OS noise
.vscode/
.idea/
.DS_Store
